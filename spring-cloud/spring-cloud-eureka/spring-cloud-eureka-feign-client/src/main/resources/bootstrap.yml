spring:  profiles:    active:    - dev
  cloud:
    config:
      discovery:        enabled: true
        service-id: spring-cloud-eureka-config-server
      username: root
      password: s3cr3t  
